GUIDELINE for run api:
  1. cd to /backend path
  2. open cmd
  3. run: uvicorn app.main:app -reload
  4. open browers and find the run server in cmd
  5. test api
Note: install PostgreSQL and setup localhost
